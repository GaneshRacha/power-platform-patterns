# Git Branch Permission & Force-Push Recovery
# Reusable Git Branch Permission & Force-Push Recovery engineering pattern.
trigger:
  branches:
    include: [ main ]

stages:
- stage: Validate
  jobs:
  - job: StaticChecks
    steps:
    - checkout: self
    - script: echo "Validate source, solution dependencies, and version contracts"
      displayName: Validate engineering artifacts

- stage: Package
  dependsOn: Validate
  jobs:
  - job: PackageSolution
    steps:
    - script: echo "Package managed solution / component artifact"
      displayName: Package
