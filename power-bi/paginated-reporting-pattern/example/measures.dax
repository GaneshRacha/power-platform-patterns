-- Paginated Reporting Pattern
-- Reusable Paginated Reporting Pattern engineering pattern.
Total Records := COUNTROWS('Fact')

Successful Records :=
CALCULATE(
    [Total Records],
    'Fact'[Outcome] = "Success"
)

Success Rate := DIVIDE([Successful Records], [Total Records], 0)
