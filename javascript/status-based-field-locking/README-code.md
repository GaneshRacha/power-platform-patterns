# Source example

`src/statusFieldLocking.ts` demonstrates a reusable, idempotent locking helper for Model-Driven Apps.

The example intentionally treats UI locking as a user-experience rule only. Dataverse security roles, field security, plugins, and server-side validation remain the authority for actual authorization and data integrity.
